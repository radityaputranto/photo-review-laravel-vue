Page: Customer Dashboard
Role: Customer

Layout: Top navbar + main content area with soft white-to-slate-50 gradient.

Navbar: Logo left, customer name + avatar right, logout icon.

Content:
- Greeting header "Hello, [Name] 👋" with subtext "Here's your session overview"
- Stats row (3 cards): Active Sessions, Photos Selected, Documents Available
  — cards with blue-50 background, icon + number + label
- Section "Your Active Sessions" — horizontal scrollable cards showing:
  each card has session name, shoot date, progress bar 
  "X / Y photos selected", status badge (Active/Submitted), 
  "View Gallery" button
- Section "Recent Documents" — simple list with file icon, 
  document name, type badge (Invoice/Contract), "Preview" button

Soft card shadows, no harsh borders.