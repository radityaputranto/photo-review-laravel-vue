Page: Sessions List
Role: Admin

Layout: Sidebar + main content.

Top:
- Title "Sessions" + "New Session" button (indigo, right side)
- Search input + filter dropdown (Status: All/Active/Completed)

Content: 
- Card-based list (not plain table) — each card:
  Left: session name (bold), customer name, shoot date
  Center: progress "X/Y photos selected" with thin progress bar
  Right: status badge + "View Detail" button + edit icon
- Pagination at bottom